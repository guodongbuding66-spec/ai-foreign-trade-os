import { assertSameOrigin, audit, json, readJson, requireAuth } from '../../_lib/auth.js';
import { dbError, normalizeTask, taskPayload } from '../../_lib/workflowdata.js';
async function current(context,tenantId,id){return context.env.DB.prepare('SELECT * FROM tasks WHERE id=? AND tenant_id=? LIMIT 1').bind(id,tenantId).first()}
export async function onRequestPut(context){const origin=assertSameOrigin(context.request);if(origin)return origin;const{response,auth}=await requireAuth(context,'crm.tasks.write');if(response)return response;const id=context.params.id,before=await current(context,auth.tenant.id,id);if(!before)return json({ok:false,error:'task_not_found'},404);let body;try{body=await readJson(context.request)}catch{return json({ok:false,error:'invalid_json'},400)}const parsed=taskPayload(body);if(parsed.error)return json({ok:false,error:parsed.error},400);const v=parsed.value;try{await context.env.DB.prepare(`UPDATE tasks SET title=?,status=?,priority=?,due_at=?,entity_type=?,entity_id=?,updated_at=? WHERE id=? AND tenant_id=?`).bind(v.title,v.status,v.priority,v.due||null,v.entityType||null,v.entityId||null,new Date().toISOString(),id,auth.tenant.id).run();const after=await current(context,auth.tenant.id,id);await audit(context,{tenantId:auth.tenant.id,userId:auth.user.id},'task.update','task',id,normalizeTask(before),normalizeTask(after));return json({ok:true,task:normalizeTask(after)})}catch(e){return dbError(e,'task_update_failed')}}
export async function onRequestDelete(context){const origin=assertSameOrigin(context.request);if(origin)return origin;const{response,auth}=await requireAuth(context,'crm.tasks.write');if(response)return response;const id=context.params.id,before=await current(context,auth.tenant.id,id);if(!before)return json({ok:false,error:'task_not_found'},404);try{await context.env.DB.prepare('DELETE FROM tasks WHERE id=? AND tenant_id=?').bind(id,auth.tenant.id).run();await audit(context,{tenantId:auth.tenant.id,userId:auth.user.id},'task.delete','task',id,normalizeTask(before),null);return json({ok:true})}catch(e){return dbError(e,'task_delete_failed')}}
