# Galaxy 生态导航验收

- 顶部“其他工具”在桌面和移动端都必须可访问。
- 菜单不得覆盖 Create / AI / 通知 / Workspace 原有动作。
- `npm run check` 必须包含 `tests/ecosystem-nav.test.mjs` 并通过。
- 所有外部工具使用 `target="_blank"` 与 `rel="noopener noreferrer"`。
